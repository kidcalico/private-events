<h1>Edit Event</h1>

<%= form_with model: @event do |form| %>
  <div>
    <%= form.label :party_title %>
    <%= form.text_field :party_title %>
  </div>

  <div>
    <%= form.label :date %>
    <%= form.datetime_field :date %>
  </div>

  <div>
    <%= form.label :location %>
    <%= form.text_field :location %>
  </div>

  <div>
    <%= form.label :more_info %>
    <%= form.text_area :more_info %>
  </div>

  <div>
    <%= form.label :visibility, "Visibility:" %>
    <%= form.select :visibility, [["Open Event", "open"], ["Invite Only", "invite_only"]] %>
  </div>

  <div>
    <span>Guest list: </span>
    <% @users.each do |user| %>
      <% unless user.id == current_user.id %>
        <div>
          <%= check_box_tag "event[attendee_ids][]", user.id, @event.attendees.include?(user), id: "attendee_#{user.id}" %>
          <%= label_tag "attendee_#{user.id}", user.username %>
        </div>
      <% end %>
    <% end %>
  </div>

  <div>
    <%= form.submit "Update Event" %>
  </div>

<% end %>